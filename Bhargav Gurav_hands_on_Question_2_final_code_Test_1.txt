import java.util.*; public class MyClass{ public static void main(String[] args){ Scanner sc = new Scanner(System.in); //int pid1, pid2, pid3, pid4, runs1, runs2, runs3, runs4; //String pnm1, pnm2, pnm3, pnm4, pt1, pt2, pt3, pt4, mt1, mt2, mt3, mt4; int playerId, runs; String playerName, playerType, matchType; Player players[] = new Player[4]; for(int i=0; i< minRuns){ minRuns = p.getruns(); ifPresent += 1; } } if(ifPresent == 0){ return 0; } return minRuns; } public static Player[] findPlayerByMatchType(Player obj[], String str){ //System.out.println("In second"); List arr = new ArrayList<>(); for(Player p : obj){ if((p.getmatchType()).equals(str)){ // System.out.println("in if"); arr.add(p); } } Collections.sort(arr, new PlayerComparator()); if(arr.size() == 0){ return null;} else{ return arr.toArray(new Player[0]);} } } class Player{ private int playerId; private int runs; private String playerName; private String playerType; private String matchType; public Player(int playerId, int runs, String playerName, String playerType, String matchType){ this.playerId = playerId; this.runs = runs; this.playerName = playerName; this.playerType = playerType; this.matchType = matchType; } public int getplayerId(){ return this.playerId; } public int getruns(){ return this.runs; } public String getPlayerName(){ return this.playerName; } public String getplayerType(){ return this.playerType; } public String getmatchType(){ return this.matchType; } public void setplayerId(int playerId){ this.playerId = playerId; } public void setruns(int runs){ this.runs = runs; } public void setplayerName(String playerName){ this.playerName = playerName; } public void setplayerType(String playerType){ this.playerType = playerType; } public void setmatchType(String matchType){ this.matchType = matchType; } } class PlayerComparator implements Comparator{ @Override public int compare(Player p1, Player p2){ return p2.getplayerId() - p1.getplayerId(); } }